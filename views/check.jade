extends layout

block content
  h3(id='checking').
    #[i(id='status-icon' class='fa fa-circle-o-notch fa-spin orange')]
    Checking #[span(id='domain' class='orange') !{domain}]
  br
  ul(id='results', class='fa-ul')
  //
    li
      i(class='fa-li fa fa-check-square' style='color:green')
      = 'Is the website only served over https?'
      br
      br
      em Test:
      pre.
        $ curl -s -I http://example.org | grep '^HTTP'
        HTTP/1.1 301 Moved Permanently
        $ curl -s -I https://example.org | grep '^HTTP'
        HTTP/1.1 200 OK